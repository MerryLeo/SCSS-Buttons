{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Run Site",
            "file": "${workspaceFolder}/index.html",
            "preLaunchTask": "Compile SASS"
        }
        
    ],
}